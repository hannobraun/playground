# Using the binding operator `=>`, you can pop values from the stack and bind
# names to them, creating a binding.
1 2 => a b .
a 1 = assert
b 2 = assert

# Bindings can shadow the name of an instrinsic function.
1 => + .
+ 1 = assert

# Bindings can also shadow bindings that have been defined earlier.
1 => a .
2 => a .
a 2 = assert
