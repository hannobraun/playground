[package]
name = "monobloc"
version = "0.0.0"
edition = "2024"

[lints.rust]
missing_docs = "warn"

[dependencies]
thiserror = "2.0.19"

[dev-dependencies]
anyhow = "1.0.104"
arbtest = "0.3.2"
num_enum = "0.7.6"

[dev-dependencies.arbitrary]
version = "1.4.2"
features = ["derive"]
